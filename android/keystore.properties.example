# 复制为 keystore.properties 后填写本机项目开发密钥信息。
# keystore.properties 已被 Git 忽略；不要提交真实路径或密码。
# Windows 路径必须使用正斜杠，或把每个反斜杠写成双反斜杠。
storeFile=D:/path/outside/repository/wakemove-development.jks
storePassword=replace-with-local-password
keyAlias=wakemove-development
keyPassword=replace-with-local-password
